# The site ships two self-contained language twins (DE at /, EN at /en/) and
# matching og-image templates. Their parallel markup and inlined CSS are the
# architecture, not copy-paste debt, so exclude them from duplication detection.
sonar.cpd.exclusions=public/index.html,public/en/index.html,scripts/og-template.html,scripts/og-template-en.html
